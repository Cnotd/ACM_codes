#include <bits/stdc++.h>
#define IOS std :: ios::sync_with_stdio(0); std :: cin.tie(0); std ::cout.tie(0);
#define PI 3.1415926
using i64 = long long;




void solve()
{
    int n , p ;
    std :: cin >> n >> p;
    std :: vector<i64> arr(n + 2);
    for(int i = 1;i <= n;i++)
    {
        std :: cin >> arr[i];
    }

    while(p--)
    {
        int l , r , z ;
        std :: cin >> l >> r >> z;

    }

    
}

int main() {
    IOS

    int _t =  1;
    //std :: cin >> _t;

    while (_t--)
        solve();
}